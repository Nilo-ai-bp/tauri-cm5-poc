{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the local CM5 POC window.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
